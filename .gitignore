# build products
gen-bitmap
imagegcc
imageqt
*.exe
*.o
.qtobj/
qtmain.moc
dist/

# GRX, when vendored or fetched by the Windows workflow
grx-build/

# what the program writes while it runs
output.png
table.png
datetime.txt
locators.js.cp866
# config/palettes/*.pal are generated by mkpalettes.py and tracked on purpose:
# the build machine does not need the pycao tree to have colours

# python bytecode from running the generator / decoder
__pycache__/
*.pyc

# smoke test renders (tests/README)
renders/

# GeoTIFF test output
tiffs/
paths-test
